<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:MathGuide.Pages"
       x:Class="MathGuide.AppShell"
       FlyoutBehavior="Disabled"
       Shell.TabBarBackgroundColor="{StaticResource Surface}"
       Shell.TabBarForegroundColor="{StaticResource Primary}"
       Shell.TabBarTitleColor="{StaticResource Primary}"
       Shell.TabBarUnselectedColor="{StaticResource TextMuted}">

    <TabBar>
        <ShellContent Title="Главная"
                      Icon="home.png"
                      Route="home"
                      ContentTemplate="{DataTemplate pages:HomePage}" />

        <ShellContent Title="Правила"
                      Icon="book.png"
                      Route="rules"
                      ContentTemplate="{DataTemplate pages:RulesPage}" />

        <ShellContent Title="Калькулятор"
                      Icon="calculator.png"
                      Route="calc"
                      ContentTemplate="{DataTemplate pages:CalculatorPage}" />
    </TabBar>
</Shell>
